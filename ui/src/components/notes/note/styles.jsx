import styled from "styled-components"

export const NoteContainer = styled.div`
    position: absolute;
    border: 1px dashed gray;
    background: #FFF;
    padding: 0.5rem 1rem;
    cursor: move;
`